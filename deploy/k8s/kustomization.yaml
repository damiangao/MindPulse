apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mindpulse

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - backend-deployment.yaml
  - frontend-deployment.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: mindpulse
  app.kubernetes.io/managed-by: kustomize
